export default function HomePage() {
  return (
    <main style={{padding: 24}}>
      <h1>Next.js migration scaffold</h1>
      <p>Root page is up. UI migration will move your existing HTML/CSS/JS next.</p>
      <p>Test the proxy route at <code>/route/secureproxy?e=ping_proxy</code> once dev server is running.</p>
    </main>
  );
}
